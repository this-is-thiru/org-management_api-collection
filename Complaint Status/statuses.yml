info:
  name: statuses
  type: http
  seq: 2

http:
  method: GET
  url: "{{ORG_MNGT}}/complaint-service/complaint-status/all/client/:clientId"
  params:
    - name: clientId
      value: KANHA1
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
