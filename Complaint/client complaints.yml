info:
  name: client complaints
  type: http
  seq: 4

http:
  method: get
  url: http://localhost:8080/complaints/all/client/:clientId
  params:
    - name: clientId
      value: KANHA1
      type: path
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
