info:
  name: complaints | by status
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8080/complaint-service/complaints/client/:clientId/status/:statusId
  params:
    - name: clientId
      value: KANHA1
      type: path
    - name: statusId
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
